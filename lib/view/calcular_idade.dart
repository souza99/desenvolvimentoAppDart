import 'package:flutter/material.dart';

class CalcularIdade extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CalcularvIdade'),
      ),
    );
  }
}
